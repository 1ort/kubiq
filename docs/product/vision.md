# Vision

Kubiq — компактный CLI для SQL-подобных запросов к Kubernetes API,
который одинаково работает с core-ресурсами и CRD.

Цели:

- Упростить отладку ресурсов в кластере
- Унифицировать выборки по разным API groups
- Дать минимальный, предсказуемый DSL (`where` + `select`) для MVP
